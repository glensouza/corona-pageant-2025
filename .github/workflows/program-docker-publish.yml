name: Program Docker Image CI

on:
  push:
    branches:
      - "main" 
    paths:
      - "Corona.Pageant.Program/**"
      - "Corona.Pageant.Shared/**"
      - ".github/workflows/program-docker-publish.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true          

jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/coronapageant25program
          tags: latest
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Corona.Pageant/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # run_onprem:
  #   runs-on: self-hosted
  #   steps:
  #     - name: Check out the repo
  #       uses: actions/checkout@v3
        
  #     - name: Publish latest containers
  #       run: |
  #         docker ps -aq | xargs docker stop | xargs docker rm
  #         docker-compose pull
  #         docker-compose up -d
